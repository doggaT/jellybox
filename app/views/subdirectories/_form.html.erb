<%= form_with(model: subdirectory) do |form| %>
  <% if subdirectory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subdirectory.errors.count, "error") %> prohibited this subdirectory from being saved:</h2>

      <ul>
        <% subdirectory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
