<%= form_with(model: file_node) do |form| %>
  <% if file_node.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(file_node.errors.count, "error") %> prohibited this file_node from being saved:</h2>

      <ul>
        <% file_node.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
